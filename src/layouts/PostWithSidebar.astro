---
import Base from "./Base.astro";
import Sidebar from "../components/Sidebar.astro";
const { frontmatter } = Astro.props;
---
<Base title={`${frontmatter.title} — bulw4rk`} description={frontmatter.description}>
  <div class="two-col">
    <div class="sidebar"><Sidebar /></div>
    <article class="post">
      <h1 style="margin:0 0 .25em">{frontmatter.title}</h1>
      {frontmatter.pubDate && (
        <p class="muted" style="margin:0 0 1.25em">
          {new Date(frontmatter.pubDate).toLocaleDateString()}
        </p>
      )}
      <slot />
    </article>
  </div>
</Base>
